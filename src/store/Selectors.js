export const albumsSelector = ({albums}) => albums.albumList;

export const loadingAlbumsSelector = ({albums}) => albums.loadingAlbums;

export const loadingPhotosSelector = ({albums}) => albums.loadingPhotos;